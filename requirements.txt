# Pinned to the versions approved for this environment. Do not bump without
# checking that list first -- an unapproved version will not install from the
# internal index.
#
# Django is held at 4.2 for the same reason. Note that 4.2 LTS reached end of
# extended support in April 2026, so it no longer receives security fixes;
# raising that with whoever owns the approved list is worth doing.
#
# The code must also stay importable on Python 3.9, which is what the
# development boxes run. `tools/check_py39_compat.py` enforces that -- see the
# note there about which syntax is off limits.

# Direct dependencies
Django==4.2.30
djangorestframework==3.16.1
django-filter==25.1
drf-spectacular==0.30.0
drf-spectacular-sidecar==2026.8.1
gunicorn==23.0.0
whitenoise==6.11.0
PyYAML==6.0.3
requests==2.32.5

# Transitive -- pinned for reproducible image builds
asgiref==3.11.1
attrs==26.1.0
certifi==2026.7.22
charset-normalizer==3.4.9
idna==3.18
inflection==0.5.1
jsonschema==4.25.1
jsonschema-specifications==2025.9.1
packaging==26.2
referencing==0.36.2
rpds-py==0.27.1
sqlparse==0.5.5
uritemplate==4.2.0
urllib3==2.6.3
